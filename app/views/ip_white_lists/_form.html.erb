<%= form_with(model: ip_white_list) do |form| %>
  <% if ip_white_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ip_white_list.errors.count, "error") %> prohibited this ip_white_list from being saved:</h2>

      <ul>
        <% ip_white_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ip %>
    <%= form.text_field :ip, class: "form-control", readonly: ip_white_list.protect %>
  </div>

  <div class="field">
    <%= form.label :expired_at %>
    <%= form.date_select :expired_at, {default: Date.today + 3.days},
	{class: "form-control", style: 'display: inline-block;width: auto;', disabled: ip_white_list.protect } %>
  </div>

  <div class="field">
    <%= form.label :memo %>
    <%= form.text_field :memo, class: "form-control", readonly: ip_white_list.protect %>
  </div>

  <div class="field">
    <%= form.label :protect %>
    <%= form.check_box :protect, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-warning' %>
  </div>
<% end %>
